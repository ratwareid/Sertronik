<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ratwareid.sertronik">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.user.order.OrderPickupActivity"></activity>
        <activity android:name=".activity.user.order.PickupServiceActivity" />
        <activity android:name=".activity.home.MitraActivity" />
        <activity android:name=".activity.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.register.RegisterActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD2h1qdNbwKJPg3In5Io8RTThO2f5Ngdlo" />

        <activity android:name=".activity.user.order.NearbyServiceActivity" />
        <activity android:name=".activity.user.SelectedCategoryActivity" />
        <activity android:name=".activity.admin.AddNewCategoryActivity" />
        <activity android:name=".activity.home.HomeActivity" />
        <activity android:name=".activity.auth.PhoneAuthActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>